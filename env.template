# Data Science Agent Environment Variables Template
# Copy this file to .env and fill in your API keys

# ============================================================
# REQUIRED: API Keys
# ============================================================

# OpenAI API Key (required for GPT models)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Google/Gemini API Key (required for ensemble mode)
# Get from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-google-gemini-key-here

# ============================================================
# ENSEMBLE MODE (Multi-Agent Voting)
# ============================================================

# Enable ensemble (calls both GPT-5 + Gemini, votes on best response)
# Set to "true" to enable, "false" or omit to disable
USE_ENSEMBLE=false

# ============================================================
# MODEL SELECTION
# ============================================================

# OpenAI model to use (default: gpt-5)
# Options: gpt-5, gpt-5-mini, gpt-5-nano, gpt-4-turbo, gpt-4o, gpt-4o-mini
OPENAI_MODEL=gpt-5

# Gemini model to use (default: gemini-2.0-flash-exp)
# Options: gemini-2.0-flash-exp, gemini-2.0-flash, gemini-pro
GENAI_MODEL=gemini-2.0-flash-exp

# ============================================================
# OPTIONAL: LiteLLM Configuration
# ============================================================

# Max retry attempts for API calls (default: 4)
LITELLM_MAX_RETRIES=4

# Request timeout in seconds (default: 60)
LITELLM_TIMEOUT_SECONDS=60

# ============================================================
# OPTIONAL: Context Window Management
# ============================================================

# Maximum context window tokens (default: 128000)
MAX_CONTEXT_TOKENS=128000

# Safety margin for context (default: 0.85 = 85%)
CONTEXT_SAFETY_MARGIN=0.85

# ============================================================
# FOLDER STRUCTURE (Cross-Platform Compatible)
# ============================================================

# Root directory for uploaded files and workspaces
# Default: .uploaded (creates .uploaded/_workspaces/<dataset>/<timestamp>/ structure)
# Supports both relative and absolute paths, tilde (~) expansion
AGENT_UPLOAD_DIR=.uploaded

# ADVANCED: Override individual workspace subdirectories (rarely needed)
# By default, everything is organized under {AGENT_UPLOAD_DIR}/_workspaces/<dataset>/<timestamp>/
# Only uncomment these if you need files in completely different locations:

# Workspace root directory (optional, defaults to {AGENT_UPLOAD_DIR}/_workspaces)
# AGENT_WORKSPACES_DIR=~/Documents/DataScienceAgent/workspaces

# Models directory (optional, defaults to workspace/models)
# Useful for: Storing models on external drive with more space
# AGENT_MODELS_DIR=/Volumes/ExternalDrive/models

# Reports/Exports directory (optional, defaults to workspace/reports)
# Useful for: Syncing reports to Dropbox/iCloud
# AGENT_REPORTS_DIR=~/Dropbox/DataScienceReports

# Plots/Visualizations directory (optional, defaults to workspace/plots)
# AGENT_PLOTS_DIR=~/Documents/DataScienceAgent/plots

# Temp directory for DuckDB spill (large datasets)
# DUCKDB_TEMP_DIR=/tmp/duckdb_spill

# ============================================================
# macOS RECOMMENDED PATHS (uncomment if on Mac)
# ============================================================

# macOS users: Uncomment this for Mac-friendly location:
# AGENT_UPLOAD_DIR=~/Documents/DataScienceAgent
# This creates the same structure as Windows:
# ~/Documents/DataScienceAgent/.uploaded/_workspaces/<dataset>/<timestamp>/
#   ├── uploads/
#   ├── data/
#   ├── models/
#   ├── reports/
#   ├── plots/
#   └── ...

# Optional: Custom temp directory for large datasets
# DUCKDB_TEMP_DIR=~/Library/Caches/DataScienceAgent/duckdb

# ============================================================
# LINUX RECOMMENDED PATHS (uncomment if on Linux)
# ============================================================

# Linux users: Uncomment this for XDG-compliant location:
# AGENT_UPLOAD_DIR=~/.local/share/data-science-agent
# This creates the same structure as Windows:
# ~/.local/share/data-science-agent/.uploaded/_workspaces/<dataset>/<timestamp>/
#   ├── uploads/
#   ├── data/
#   ├── models/
#   ├── reports/
#   ├── plots/
#   └── ...

# Temp directory (Linux default is fine)
# DUCKDB_TEMP_DIR=/tmp/duckdb_spill

# ============================================================
# HOW TO USE
# ============================================================

# 1. Copy this file:
#    cp env.template .env

# 2. Edit .env with your actual API keys

# 3. (Optional) Uncomment and customize folder paths above

# 4. Start server:
#    python start_server.py

# 5. The code will automatically load .env file

